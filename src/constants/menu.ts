import { MenuItem } from "../../types/menu";

export const menu: MenuItem[] = [
  { label: "Inicio", path: "/" },
  { label: "Emitir", path: "/emitir" },
  { label: "Retirar", path: "/retirar" },
  { label: "Migrar", path: "/migrar" },
  { label: "Exchange", path: "/exchange" }
];

export default menu;
